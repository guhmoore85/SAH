name: cross_channel
version: '1.0.0'
config-version: 2

profile: cross_channel

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  cross_channel:
    staging:
      +materialized: view
    marts:
      +materialized: table
