name: everyaction
version: '1.0.0'
config-version: 2

profile: everyaction

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  everyaction:
    staging:
      +materialized: view
    marts:
      +materialized: view
