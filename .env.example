# Google Sheets Configuration
GOOGLE_SHEET_ID=12teYXqd8kBIFRorrndQMmYIy0gp2D5FLhfeq-p8-l5I
GOOGLE_SHEET_TAB=Airtable_Active_365

# Google Service Account Credentials
# Option 1: Path to JSON key file (for local development)
GOOGLE_SERVICE_ACCOUNT_FILE=path/to/service-account.json

# Option 2: JSON string (for GitHub Actions - use secrets)
# GOOGLE_SERVICE_ACCOUNT_JSON={"type": "service_account", ...}

# Airtable Configuration
AIRTABLE_BASE_ID=appdZ68evU4qlG8mg
AIRTABLE_TABLE_ID=tbl15Hfjy2CZTqgV6
AIRTABLE_PAT=your_airtable_personal_access_token_here

# Optional: Limit rows for testing (0 = no limit)
ROW_LIMIT=0

# ---------------------------------------------------------------------------
# Gmail → BigQuery Pipeline (gmail_to_bigquery.py)
# ---------------------------------------------------------------------------

# BigQuery project and dataset
BQ_PROJECT=stopaapihate-472516
BQ_DATASET=everyaction_reports

# Gmail user to impersonate via domain-wide delegation
GMAIL_USER=hmoore@stopaapihate.org

# Label applied to processed emails (auto-created if missing)
GMAIL_PROCESSED_LABEL=BQ-Processed

# ---------------------------------------------------------------------------
# EveryAction → Airtable Sync (sync_everyaction_airtable.py)
# ---------------------------------------------------------------------------

# Daily summaries Airtable table (consolidated: daily_full_list + sah_donors + daily_sah_365)
EA_SUMMARIES_BASE_ID=your_airtable_base_id
EA_SUMMARIES_TABLE_ID=your_airtable_table_id

# Email comparison detail table (optional)
EA_EMAIL_COMPARISON_BASE_ID=your_airtable_base_id
EA_EMAIL_COMPARISON_TABLE_ID=your_airtable_table_id

# Forms report detail table (optional)
EA_FORMS_REPORT_BASE_ID=your_airtable_base_id
EA_FORMS_REPORT_TABLE_ID=your_airtable_table_id
